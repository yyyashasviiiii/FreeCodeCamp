function randomWholeNum() {

  // Only change code below this line

  return Math.floor(Math.random() * 10);
} // according to the question we multiply the expression by 10 as the whole number must be between 0 and 9.


//       END
